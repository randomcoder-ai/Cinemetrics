import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Movie Sentiment Analyzer - AI-Powered Audience Insights",
  description: "Discover what audiences really think about movies with AI-powered sentiment analysis. Enter an IMDb ID to get comprehensive insights, ratings, and reviews.",
  keywords: ["movie", "sentiment", "analysis", "IMDb", "reviews", "AI", "ratings"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
